== Procedure

include::generic-service-nav.adoc[]

. Click *Bind to App* and follow the *Create mobile client* wizard to associate the {mobile-client} with the {service-name} Service.
+
NOTE: When selecting the binding parameters, use `public` when binding a {mobile-client} to a {service-name}. When binding mobile services to each other, use `bearer`.
